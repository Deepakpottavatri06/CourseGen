# secret.yaml.example 
apiVersion: v1
kind: Secret
metadata:
  name: coursegen-secret
type: Opaque
stringData:
  DATABASE_URL: "<set via kubectl create secret>"
  SECRET_KEY: "<set via kubectl create secret>"
  GEMINI_API_KEY: "<set via kubectl create secret>"
